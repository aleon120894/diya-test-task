package tests;

import io.appium.java_client.AppiumDriver;

public class Login {
    //
    AppiumDriver driver = new AppiumDriver();
    Elements elements = new Elements();
    driver.click(elements.submitButton);
}
